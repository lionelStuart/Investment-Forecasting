# Agent Entry

This project uses the gstack project-system memory layout.

Agents should start from `repo/AGENTS.md`, then follow the active task listed in
`repo/STATUS.md`. Do not implement directly from the README alone.

The README defines the product goal. The `repo/` directory translates that goal
into durable specs, executable tasks, acceptance criteria, and write-back rules.

